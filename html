<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Infinity Scientific Calculator</title>

<!-- FUTURE FONTS -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Orbitron:wght@400;600&display=swap" rel="stylesheet">

<style>
/* =========================
   FUTURE LIFE DESIGN SYSTEM
========================= */
:root {
  --bg-dark: #070b14;
  --bg-light: #f4f6fb;
  --glass-dark: rgba(255,255,255,0.08);
  --glass-light: rgba(255,255,255,0.6);
  --neon: #00f7ff;
  --accent: #6cf2c2;
  --danger: #ff6b6b;
  --text-dark: #eaf4ff;
  --text-light: #111;
  --blur: blur(18px);
}

body {
  margin: 0;
  font-family: 'Inter', sans-serif;
  background: radial-gradient(circle at top, #0c1630, var(--bg-dark));
  color: var(--text-dark);
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  overflow-x: hidden;
}

/* =========================
   MAIN CONTAINER
========================= */
.app {
  width: 100%;
  max-width: 420px;
  background: var(--glass-dark);
  backdrop-filter: var(--blur);
  border-radius: 26px;
  padding: 16px;
  box-shadow: 0 0 60px rgba(0,255,255,0.15);
  animation: float 6s ease-in-out infinite;
}

@keyframes float {
  0%,100% { transform: translateY(0); }
  50% { transform: translateY(-6px); }
}

/* =========================
   HEADER
========================= */
header {
  text-align: center;
  font-family: 'Orbitron', sans-serif;
  font-size: 1.1rem;
  letter-spacing: 1px;
  color: var(--neon);
  margin-bottom: 10px;
}

/* =========================
   DISPLAY
========================= */
.display {
  background: rgba(0,0,0,0.45);
  border-radius: 18px;
  padding: 14px;
  font-size: 1.5rem;
  min-height: 70px;
  word-wrap: break-word;
  box-shadow: inset 0 0 12px rgba(0,255,255,0.15);
}

/* =========================
   BUTTON GRID
========================= */
.buttons {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 10px;
  margin-top: 14px;
}

button {
  background: rgba(255,255,255,0.08);
  border: none;
  border-radius: 14px;
  padding: 14px;
  color: white;
  font-size: 1rem;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  transition: transform .15s, box-shadow .15s;
}

button:hover {
  box-shadow: 0 0 12px var(--neon);
}

button:active {
  transform: scale(0.95);
}

/* Ripple */
button::after {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(circle, rgba(0,255,255,0.35), transparent 60%);
  opacity: 0;
  transition: opacity .2s;
}
button:active::after {
  opacity: 1;
}

.op { color: var(--accent); }
.equal { background: linear-gradient(135deg, var(--neon), #3bffff); color:#000; }
.clear { color: var(--danger); }

/* =========================
   PANELS
========================= */
.panel {
  margin-top: 14px;
  background: rgba(0,0,0,0.35);
  border-radius: 18px;
  padding: 12px;
  max-height: 200px;
  overflow-y: auto;
  display: none;
  animation: fadeIn .3s ease;
}

@keyframes fadeIn {
  from { opacity:0; transform: translateY(6px); }
  to { opacity:1; transform: translateY(0); }
}

.panel h3 {
  margin-top:0;
  color: var(--neon);
}

/* =========================
   FOOTER
========================= */
footer {
  text-align:center;
  font-size:.75rem;
  margin-top:12px;
  opacity:.7;
}
</style>
</head>
<body>

<div class="app">
  <header>∞ Infinity Scientific Calculator</header>

  <div id="display" class="display">0</div>

  <div class="buttons">
    <button class="clear" onclick="clearAll()">AC</button>
    <button onclick="append('(')">(</button>
    <button onclick="append(')')">)</button>
    <button onclick="append('π')">π</button>
    <button onclick="append('e')">e</button>

    <button onclick="append('sin(')">sin</button>
    <button onclick="append('cos(')">cos</button>
    <button onclick="append('tan(')">tan</button>
    <button onclick="append('log(')">log</button>
    <button onclick="append('ln(')">ln</button>

    <button onclick="append('7')">7</button>
    <button onclick="append('8')">8</button>
    <button onclick="append('9')">9</button>
    <button class="op" onclick="append('/')">÷</button>
    <button onclick="append('√(')">√</button>

    <button onclick="append('4')">4</button>
    <button onclick="append('5')">5</button>
    <button onclick="append('6')">6</button>
    <button class="op" onclick="append('*')">×</button>
    <button onclick="append('^')">xʸ</button>

    <button onclick="append('1')">1</button>
    <button onclick="append('2')">2</button>
    <button onclick="append('3')">3</button>
    <button class="op" onclick="append('-')">−</button>
    <button onclick="append('!')">!</button>

    <button onclick="togglePanel('rules')">📘</button>
    <button onclick="append('0')">0</button>
    <button onclick="append('.')">.</button>
    <button class="op" onclick="append('+')">+</button>
    <button class="equal" onclick="calculate()">=</button>
  </div>

  <!-- MATHEMATICAL KNOWLEDGE -->
  <div id="rules" class="panel">
    <h3>Mathematical Laws & Formulas</h3>

    <b>Laws of Indices</b><br>
    aᵐ × aⁿ = aᵐ⁺ⁿ<br>
    <i>Example:</i> 2² × 2³ = 2⁵ = 32<br><br>

    <b>Trigonometry</b><br>
    sin²θ + cos²θ = 1<br>
    <i>Example:</i> sin²30° + cos²30° = 1<br><br>

    <b>Logarithms</b><br>
    log(ab) = log a + log b<br>
    <i>Example:</i> log(10×100)=1+2=3<br><br>

    <b>Algebra</b><br>
    (a+b)² = a² + 2ab + b²<br><br>

    <b>Geometry</b><br>
    Area of Circle = πr²
  </div>

  <footer>
    Developed by Gamaliel Paul
  </footer>
</div>

<script>
/* =========================
   SAFE SCIENTIFIC ENGINE
========================= */
const display = document.getElementById("display");

function append(val){
  if(display.innerText === "0") display.innerText = "";
  display.innerText += val;
}

function clearAll(){
  display.innerText = "0";
}

function togglePanel(id){
  const panel = document.getElementById(id);
  panel.style.display = panel.style.display === "block" ? "none" : "block";
}

/* SAFE PARSER */
function calculate(){
  try {
    let expr = display.innerText
      .replace(/π/g, "Math.PI")
      .replace(/e/g, "Math.E")
      .replace(/√/g, "Math.sqrt")
      .replace(/sin/g, "Math.sin")
      .replace(/cos/g, "Math.cos")
      .replace(/tan/g, "Math.tan")
      .replace(/log/g, "Math.log10")
      .replace(/ln/g, "Math.log")
      .replace(/\^/g, "**")
      .replace(/(\d+)!/g, "fact($1)");

    const result = Function("fact","return "+expr)(factorial);
    display.innerText = result;
  } catch {
    display.innerText = "Error";
  }
}

function factorial(n){
  if(n<0) return NaN;
  return n<=1 ? 1 : n*factorial(n-1);
}

/* KEYBOARD SUPPORT */
document.addEventListener("keydown", e=>{
  if("0123456789.+-*/()".includes(e.key)) append(e.key);
  if(e.key==="Enter") calculate();
  if(e.key==="Backspace") display.innerText = display.innerText.slice(0,-1) || "0";
});
</script>

</body>
  </html>
  
